# Task Completion Checklist

When completing a task:

1. **Type check:** `bun run build` (uses tsc via noEmit)
2. **Test run:** `bun run dev` to verify functionality
3. **Lint:** (to be configured)
4. **Format:** (to be configured)

## Before Commit

- Ensure no `any` types
- Remove debug console.log statements
- Verify no secrets/credentials in code
